{
  "name": "page-reference-to-text",
  "version": "0.1",
  "description": "A plugin that converts Page References to text to allow for easy export.",
  "main": "dist/index.html",
  "targets": {
    "main": false
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "parcel build --no-source-maps index.html --public-url ./"
  },
  "keywords": [],
  "author": "B.Finch",
  "license": "MIT",
  "dependencies": {
    "@logseq/libs": "^0.0.17"
  },
  "logseq": {
    "id": "page-reference-to-text",
    "title": "page-reference-to-text",
    "icon": "./icon.png"
  },
  "devDependencies": {
    "parcel": "^2.0.0"
  }
}
